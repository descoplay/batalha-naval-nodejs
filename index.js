// const Tabuleiro = require('./Tabuleiro')
const Pecas = require('./Pecas')

console.log('BATALHA NAVAL')
console.log()

// Tabuleiro.gerar()

Promise.resolve()
    .then(() => {
        return Pecas.setPos()
    })